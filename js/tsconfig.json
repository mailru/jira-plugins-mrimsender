{
  "extends": "@atlascommunity/tsconfig-base",
  "compilerOptions": {
    "lib": [
      // ES6
      "DOM",
      "ES6",
      "DOM.Iterable",
      "ScriptHost",
      // Additional
    ]
  },
  "include": [
    "typings/*.d.ts",
    "src/**/*.ts",
    "src/**/*.tsx"
  ],
  "exclude": [
    "node_modules"
  ]
}